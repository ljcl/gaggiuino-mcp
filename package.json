{
  "name": "gaggiuino-mcp",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:affected": "turbo run build --affected",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "typecheck:affected": "turbo run typecheck --affected",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "dev": "turbo run dev",
    "boundaries": "turbo boundaries",
    "check": "turbo run lint test typecheck build knip && turbo boundaries",
    "check:affected": "turbo run lint test typecheck build knip --affected && turbo boundaries",
    "knip": "knip",
    "knip:summary": "knip --reporter json --no-exit-code | bun scripts/knip-summary.ts",
    "storybook": "turbo run storybook --filter=@gaggiuino/storybook",
    "build-storybook": "turbo run build:storybook --filter=@gaggiuino/storybook"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "knip": "6.27.0",
    "turbo": "2.10.5"
  }
}
